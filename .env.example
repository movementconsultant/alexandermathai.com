# Copy to .env for local development. Never commit a real .env file.

# Preview / noindex convention.
# Any value other than the literal string "false" is treated as preview mode:
# the site ships <meta name="robots" content="noindex, nofollow">, an empty
# sitemap, and no canonical link to the live production domain.
# This defaults to preview mode when unset — see docs/LAUNCH_BLOCKERS.md for
# what has to happen before this can safely be set to "false".
PUBLIC_PREVIEW=true
# Copy to .env for local builds.
#
# PUBLIC_PREVIEW=true  -> every page gets <meta name="robots" content="noindex, nofollow">,
#                          robots.txt disallows all crawling, and no page is treated as indexable.
#                          Use this for ALL preview/staging builds, including this one.
# PUBLIC_PREVIEW=false -> production behavior: indexable, full robots.txt allow + sitemap
#                          reference. Only flip this for the actual production deploy, and only
#                          once the items in docs/mark-2-release-audit.md are resolved or
#                          explicitly accepted by the site owner.
PUBLIC_PREVIEW=true

# The /contact form has no submission endpoint of any kind and cannot send, collect, or store
# data — this is intentional, not a missing env var. There is no owner-approved, verified
# delivery backend yet. Connecting one requires an actual code change to src/pages/contact.astro
# (not just an environment variable), by design — see docs/mark-2-launch-plan.md
# "Contact-form decision".

# Mark 29 — gates the Consulting page's "Purchase" buttons (see
# src/components/ui/PurchaseButton.astro). Unset (the default in every
# build this repo runs today) -> buttons render as a static "Contact to
# Purchase" link to /contact instead of a live Stripe Checkout flow; no
# fetch call exists in that state at all. This points at the SAME shared
# Stripe checkout Worker as texasmovement.com/consulting — one Texas
# Movement Consulting practice, one checkout — not a separate AVM-branded
# endpoint. See texasmovement.com's workers/stripe-checkout/README.md for
# the deployment sequence.
# PUBLIC_STRIPE_CHECKOUT_ENDPOINT=https://tmi-stripe-checkout.<your-subdomain>.workers.dev
