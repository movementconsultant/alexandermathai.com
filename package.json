{
  "name": "alexandermathai.com",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Placeholder site for Alexander Mathai — founder voice and authority layer for the Texas Movement ecosystem.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "postbuild": "node scripts/postbuild-guard.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --test tests/*.test.mjs",
    "test:a11y": "node scripts/a11y-scan.mjs"
  },
  "dependencies": {
    "astro": "7.2.2"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "@axe-core/playwright": "4.13.0",
    "playwright": "1.62.1",
    "typescript": "6.0.3"
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check && astro build",
    "postbuild": "node scripts/postbuild-guard.mjs",
    "guard": "node scripts/postbuild-guard.mjs",
    "test:e2e:preview": "npm run build && playwright test tests/e2e/preview.spec.ts",
    "test:e2e:production": "PUBLIC_PREVIEW=false npm run build && playwright test tests/e2e/production.spec.ts",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "format": "prettier --check .",
    "format:write": "prettier --write ."
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.5",
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@playwright/test": "^1.62.1",
    "prettier": "^3.9.6",
    "prettier-plugin-astro": "^0.14.1",
    "typescript": "^6.0.3"
  }
}
